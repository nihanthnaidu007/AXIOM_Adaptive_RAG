# Recent Transformer Architecture Improvements

The latest transformer architecture improvements focus on attention
efficiency. FlashAttention computes exact attention with an IO-aware memory
layout, a major efficiency improvement for long contexts.

Other recent improvements include weight quantization, knowledge distillation
into smaller architectures, rotary position embeddings, and grouped-query
attention.

These transformer improvements let attention models run with lower latency
and memory on longer contexts.
